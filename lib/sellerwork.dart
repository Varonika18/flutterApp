import 'package:flutter/material.dart';

class Sellerwork extends StatelessWidget {
  const Sellerwork({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}